## 1. Fast Markdown Worker Boundary

- [x] 1.1 [P0][Depends: none][Input: `vite.config.ts`, `fastMarkdown.worker.ts`, `compile.ts`, `sanitize.ts`, `workerAdapter.ts` current import graph][Output: precise DOM-free conditional dependency resolution + unsafe Worker artifact + explicit main-thread sanitization/fallback boundary][Verify: production Worker bundle contains no runtime `document/window` dependency on the ordinary compile path] Split the existing compile pipeline without adding a dependency or DOM shim.
- [x] 1.2 [P0][Depends: 1.1][Input: existing fast Markdown fixtures and Worker adapter tests][Output: real Worker smoke, stale-result, bounded timeout/pending cleanup, sanitizer/XSS and fallback regression coverage][Verify: focused Vitest/build Worker tests pass and ordinary Markdown does not enter main-thread compile fallback] Lock the Worker boundary with runnable tests.

## 2. Timeline And Visible Streaming Stability

- [x] 2.1 [P0][Depends: none][Input: `Messages.tsx`, `MessagesTimeline.tsx`, `MessagesRows.tsx`, row/overlay diagnostics and current stable snapshot helpers][Output: stable nullable approval/input projections, engine-scoped heartbeat and recovery-only deferred-text bypass][Verify: render-count/probe test proves same-item text growth updates the live row without equivalent parent Timeline re-derivation] Remove the measured Timeline/active-row amplification without changing message identity or ordering.
- [x] 2.2 [P0][Depends: 2.1][Input: existing stream latency and render-surface tests][Output: regressions for latest visible text, final Markdown convergence, parallel thread scope and overlay no-op writes][Verify: focused Messages/stream-latency tests pass] Preserve `stable parent snapshot + live row override`.

## 3. History Reopen Continuity

- [x] 3.1 [P1][Depends: none][Input: `useThreadActionsResumeThread`, pending-id helper, history loading state and current snapshot/local/reopen outcomes][Output: pending short-circuit, single real-thread recovery guard, last-good readable fallback and explicit retryable failure surface][Verify: pending never calls backend; failed/degraded empty never replaces a readable surface or appears as successful empty] Harden history hydration without inventing new storage.
- [x] 3.2 [P1][Depends: 3.1][Input: existing resume/recovery diagnostic tests][Output: happy, degraded/failure, no-authority-empty negative contract, replacement-switch guard and retry-bound tests][Verify: focused thread action tests pass, recovery runs at most once, and no source without an explicit authority proof is treated as authoritative empty] Lock the history behavior.

## 4. Codex Model Catalog Refresh

- [x] 4.1 [P1][Depends: none][Input: `WorkspaceSession::probe_health`, session ensure/reuse callers and existing frontend model owner][Output: health probe uses supported static non-model RPC; explicit `model/list` owner remains unchanged][Verify: runtime reuse probe sends `collaborationMode/list` and never `model/list`] Remove the client-controlled extra refresh trigger.
- [x] 4.2 [P1][Depends: 4.1][Input: app-server/session runtime probe tests][Output: non-model probe regression plus explicit model-list behavior preservation][Verify: focused Rust probe tests pass] Distinguish the local trigger fix from the upstream Codex periodic refresh worker.

## 5. Error Log And Diagnostics Retention

- [x] 5.1 [P1][Depends: none][Input: `clientErrorLog.ts`, `useDebugLog.ts`, `threadAppServerEventDiagnostics.ts`][Output: privacy-safe stderr classifier and top-level string redaction][Verify: unknown raw stderr never persists content; known model timeout maps to stable signature] Close the current string-payload privacy gap.
- [x] 5.2 [P1][Depends: 5.1][Input: sanitized stderr signatures and existing append command][Output: bounded renderer-side aggregation with `count/firstSeen/lastSeen`, immediate actionable-error path and lifecycle flush][Verify: hundreds of identical stderr events create a bounded number of persisted summaries] Remove the 99.8% duplicate-log storm without suppressing evidence.
- [x] 5.3 [P2][Depends: none][Input: `rendererDiagnostics.ts` category caps, Fast Markdown Worker failure owner and startup sanitize path][Output: reserved bounded budgets for actionable error/stability, performance and stream-latency evidence plus a rate-limited content-safe Worker failure producer][Verify: frame-drop pressure cannot evict all Worker/error/stream-stall samples; Worker failures/timeouts persist without content; malformed and wrong-typed entries remain safe] Preserve forensic signal under rolling pressure.
- [x] 5.4 [P1][Depends: 5.1, 5.2, 5.3][Input: existing debug/error/renderer diagnostics tests][Output: sanitizer, aggregation, immediate error/lifecycle flush, cap and restart-maintenance regressions][Verify: focused diagnostics tests pass] Lock privacy and retention behavior.

## 6. Gemini Owned Child Lifecycle

- [x] 6.1 [P1][Depends: none][Input: Gemini command construction and current long-prompt stdin branch][Output: supported prompt launches use stdin for all prompt sizes and argv contains control args only][Verify: Rust command-plan tests prove raw prompt absent from args and present in stdin payload] Remove prompt exposure from process listings.
- [x] 6.2 [P1][Depends: 6.1][Input: Gemini normal completion, concurrent create/remove, interrupt, GUI/daemon shutdown and `Drop` paths][Output: single-owner session creation plus owned process-group remove/kill/wait or bounded reap on every teardown path, with content-safe lock-contention diagnostics][Verify: registry handle is removed; normal/timeout/removal/shutdown tests exit the root and TERM-resistant descendants; no create can win after remove/shutdown begins] Harden the existing registry rather than adding a parallel process manager.
- [x] 6.3 [P1][Depends: 6.2][Input: engine process diagnostics and stdin/stdout/stderr flow-control contracts][Output: Rust lifecycle/diagnostic regressions that keep registry and OS-liveness evidence distinct while large prompt writes and output drains make progress concurrently][Verify: targeted Gemini/engine/daemon tests pass on supported host and cross-platform code compiles] Lock ownership, flow control and evidence boundaries.

## 7. Cross-Layer Verification And Handoff

- [x] 7.1 [P0][Depends: 1.2, 2.2, 3.2, 4.2, 5.4, 6.3][Input: all touched frontend/Rust files][Output: formatted implementation with no lint/type/runtime-contract violations][Verify: focused Vitest, `npm run typecheck`, `npm run lint`, `npm run check:runtime-contracts`, targeted `cargo test` pass] Run automated quality gates.
- [x] 7.2 [P1][Depends: 7.1][Input: completed OpenSpec artifacts and implementation][Output: strict change validation and cross-layer audit][Verify: `openspec validate stabilize-client-runtime-and-diagnostics --strict --no-interactive` plus reuse/import/same-layer checks pass] Verify artifacts and implementation alignment.
- [ ] 7.3 [P1][Depends: 7.2][Input: rebuilt app with React Scan disabled and performance diagnostics enabled][Output: idle/background/foreground streaming evidence plus Worker/history/model/process smoke notes][Verify: no Worker DOM error; no first-delta visible stall >700ms; frame p95 target <30ms; history/model/process behaviors match specs] Human runtime acceptance; functional smoke passed, but keep unchecked until the quantified trace is retained.

## 8. Review-Discovered Contract Closure

- [x] 8.1 [P1][Depends: 1.2][Input: Worker success/error/timeout/unavailable paths plus document identity guard][Output: stale requests bypass finalization and main-thread fallback on every settlement path][Verify: focused tests prove stale reject/unavailable/timeout never call `compileFastMarkdown`] Close the stale fallback gap found in pre-commit review.
- [x] 8.2 [P1][Depends: 3.2][Input: history retry state, selection scheduling and overlapping loader requests][Output: canonical-thread automatic retry budget + explicit Retry re-arm + per-thread generation guard][Verify: reselect performs zero implicit calls after failure; older out-of-order response cannot overwrite newer state] Close history retry and stale-response races.
- [x] 8.3 [P1][Depends: 6.3][Input: frontend execution surfaces, settings sanitize/defaults, thread messaging/session/Tauri service owners, bulk/preferred engine detection, GUI/daemon async+sync send, session launch/remove/shutdown][Output: Gemini hard-disable policy + read-only historical thread + zero-probe detection + session launch gate + explicit cleanup failure propagation][Verify: legacy true settings normalize false; orchestration/project-map/checkpoint/task-recovery/direct send boundaries reject or normalize before execution; historical composer/queue cannot cross-send to another Provider; frontend services reject before IPC; hard-disabled bulk/preferred discovery never invokes `gemini --version`; stale Arc/pre-registration interrupt cannot spawn; failed cleanup is not reported successful] Close Gemini capability and lifecycle ownership gaps.
- [x] 8.4 [P1][Depends: 8.1, 8.2, 8.3][Input: implementation, OpenSpec artifacts and `.trellis/spec/**` executable contracts][Output: updated verification/index/spec evidence with branding-safe fixtures][Verify: focused tests, lint, typecheck, runtime contracts, `doctor:strict`, targeted Rust tests and strict OpenSpec validation pass] Re-run the zero-regression gate without committing.
